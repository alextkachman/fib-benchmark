apply plugin: 'groovy'
apply plugin: 'idea'

group = 'org.mbte.groovypp'

repositories {
    mavenRepo urls: 'http://groovypp.artifactoryonline.com/groovypp/libs-releases-local/'

    mavenCentral()
}

dependencies {
    groovy group: 'org.mbte.groovypp', name: 'groovypp-all', version:'0.4.115'
}
